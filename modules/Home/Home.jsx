import styles from "./Home.module.css";

const Home = () => {
  // const theme = useTheme();
  // let mode = theme.palette.mode;

  return <main className={styles}>Home page</main>;
};

export default Home;
